// We want to get:


// Preorder → Root → Left → Right
// Inorder → Left → Root → Right
// Postorder → Left → Right → Root

// Core Idea:

// Instead of visiting a node once…

// We visit each node 3 times using a stack

// And we track:

// (node, state)

function allTravesal(root){
  const pre = [];
  const inorder = [];
  const post = [];
}




















